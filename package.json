{
  "name": "expo-android-targets",
  "version": "1.0.0",
  "description": "Expo config plugin for Android Glance home screen widget with React Native integration",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "clean": "rm -rf build",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run clean && npm run build",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": [
    "expo",
    "expo-plugin",
    "android",
    "widget",
    "glance",
    "home-screen",
    "react-native"
  ],
  "author": "Emplica",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/emplica/expo-android-targets.git"
  },
  "files": [
    "build",
    "templates",
    "src",
    "README.md"
  ],
  "peerDependencies": {
    "expo": ">=49.0.0",
    "@expo/config-plugins": ">=7.0.0"
  },
  "dependencies": {
    "@expo/config-plugins": "^7.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
